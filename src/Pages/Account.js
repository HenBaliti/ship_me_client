import React from "react";
import styled from "styled-components";

const AccountPage = () => {
  return (
    <AccountContainer style={{ marginTop: 100 }}>
      <h1>Account</h1>
    </AccountContainer>
  );
};

const AccountContainer = styled.div``;

export default AccountPage;
